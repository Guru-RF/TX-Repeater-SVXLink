This is the install guide for the RF.Guru SVXLink TRXrptr HAT as a TX only Repeater node
First install a 64 bit raspberry os lite image
then apt install git
and clone this repository
sudo ./install-audiochip
sudo ./install-svxlink-main
./install-wireguard
config svxlink and wireguard
